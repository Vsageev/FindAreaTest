using System;

namespace FigureLib
{
    public static class Circle
    {
        public static double findArea(double radius)
        {
            return (radius * radius * Math.PI);
        }
    }
}
